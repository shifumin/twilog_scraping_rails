- if @tweet.errors.any?
  #error_explanation
    .alert.alert-danger
      The form contains
      = pluralize(@tweet.errors.count, "error")
    %ul
      - @tweet.errors.full_messages.each do |msg|
        %li<
          = msg
